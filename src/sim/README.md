# Deterministic simulation

This directory is reserved for pure TypeScript game state and transitions. It may not import React,
browser storage, DOM APIs, animation code or wall-clock time. WP-010 establishes the first contracts.
