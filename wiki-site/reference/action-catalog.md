# Action catalog

The design defines eleven base action families plus contextual actions. WP-011 validates authored
definitions and WP-020 implements availability, preview, costs, collateral, duration, visibility,
resolution, fallback, AI permissions and chronicle templates. This page intentionally avoids an
unimplemented duplicate catalog.
