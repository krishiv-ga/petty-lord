# State schema reference

WP-000 does not create gameplay state. The canonical shape includes schema/build versions, seed and
PRNG state, scheduler sequence, time, king, lords, territories, relationships, support, Church,
agreements, Orders, AI Intents, secrets, knowledge, events, decisions, chronicle, flags and ending.

WP-010 owns the first authoritative types; this page will link exact source only after integration.
