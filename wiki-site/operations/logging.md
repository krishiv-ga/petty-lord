# Logging

Every implementer and critic writes a separate evidence log using `logs/AGENT_LOG_TEMPLATE.md`.
Individual logs list exact scope, files, decisions, tests, findings, risks and handoff. Integrators
write compacted wave logs and update `logs/STATUS.md` only after the integrated SHA is fixed.
