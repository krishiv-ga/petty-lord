# Debugging

Start with the narrow failed command and preserve its seed, trace, screenshot or serialized input. Do
not replace deterministic evidence with browser timing. Playwright retains traces, screenshots and
video on failure under `test-results/` and `playwright-report/`.

The gameplay debug panel belongs to later application packets and must keep omniscient truth out of
production projections.
