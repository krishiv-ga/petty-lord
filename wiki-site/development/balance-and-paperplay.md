# Balance and paperplay

The design package includes hostile paperplay and a final amendment ledger. Gameplay exploits use the
repo-local `$hunt` workflow; values change through `$tune` only after reproducible evidence identifies
the cause. Locked rules change through `$design-guard`, never as an implementation shortcut.
