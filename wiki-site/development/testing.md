# Testing

Biome checks formatting and lint, TypeScript checks contracts, Vitest runs unit and headless simulation
suites, Playwright covers browser flows and screenshots, axe supplies automated accessibility checks,
and Storybook builds reusable state fixtures.

Run the narrowest relevant checks while iterating and the packet's full acceptance gate before critic
handoff. See [commands](../reference/commands.md).
