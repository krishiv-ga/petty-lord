# Test suites

- `unit/` covers configuration and pure helpers.
- `sim/` is the stable headless deterministic-simulation gate.
- `e2e/` contains Playwright browser flows and evidence.

Later packets add tests beside owned behavior without renaming the root command surface.
